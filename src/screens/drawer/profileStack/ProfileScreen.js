//Components
import AppView from "../../../components/atoms/AppView";

const ProfileScreen = (props) => {
  return (
    <AppView {...props}>
      
    </AppView>
  );
};

export default ProfileScreen;
